# Test configuration

# Include main config, override as needed
include "application"

pekko {
  loglevel = "WARNING"

  actor {
    provider = "local"
  }

  http {
    host-connection-pool {
      max-connections = 4
      max-open-requests = 32
    }
  }
}
