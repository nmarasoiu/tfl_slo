# Test configuration for Pekko
pekko {
  loglevel = "WARNING"

  actor {
    provider = "local"
  }

  http {
    host-connection-pool {
      max-connections = 4
      max-open-requests = 32
    }
  }
}
